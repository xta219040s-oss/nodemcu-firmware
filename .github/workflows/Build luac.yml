- name: Build luac.cross.exe
  run: |
    echo "=== Current directory ==="
    echo %CD%
    echo "=== List files ==="
    dir
    echo "=== List parent directory ==="
    dir ..\
    echo "=== Search for msvc ==="
    dir /s /ad | findstr msvc
  shell: cmd
